#include "main.h"
/**
 * _abs - computs absolute value
 *
 * 'int' _abs: int type number
 * @n: pulled from the main file
 *
 * Return: Returns result.
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
